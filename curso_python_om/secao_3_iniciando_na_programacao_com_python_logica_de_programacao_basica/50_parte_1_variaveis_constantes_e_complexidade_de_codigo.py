"""
CONSTANTE = "Variáveis" que não vão mudar
Muitas condições no mesmo if (ruim)
    <- Contagem de complexidade (ruim)
"""

# Define uma constante, que é uma variável que não será alterada durante a execução do programa
CONSTANTE = "Variáveis que não vão mudar"

# Define a velocidade atual do carro e o local em que o carro está na estrada
velocidade = 61  
local_carro = 100  

# Define as constantes relacionadas ao radar
RADAR_1 = 60  # Velocidade máxima permitida pelo radar 1
LOCAL_1 = 100  # Localização do radar 1 na estrada
RADAR_RANGE = 1  # A distância em que o radar pode pegar um carro com excesso de velocidade
